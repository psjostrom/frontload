# MCP tools — halted indefinitely

Frontload's agent-facing MCP product is halted indefinitely, and its server
entrypoint is disabled in the updated release. Older installations remain active
until updated or uninstalled. The retained handlers are historical
implementation material. Use [RTK](https://github.com/rtk-ai/rtk) instead.
