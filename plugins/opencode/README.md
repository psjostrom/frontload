# Frontload for OpenCode — halted indefinitely

This integration is halted indefinitely because Frontload has no evidence that
this unmeasured path avoids the Codex architecture's costs. Use
[RTK](https://github.com/rtk-ai/rtk) instead. See the repository root README.
